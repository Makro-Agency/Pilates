.sec-contact-map .main-page-title { margin: 0 0 120px; }

.secConMap .wrapper { display: flex; flex-direction: row; justify-content: space-between; }
  .secConMap .wrapper .content { max-width: 585px; padding-right: 50px; width: 100%; }
  .secConMap .wrapper .content:only-child { padding-right: 0; margin-left: auto; margin-right: auto; max-width: 100%; }
  .secConMap .wrapper .map-wrapper { max-width: 650px; width: 100%; position: relative; }
  .secConMap .wrapper .map-wrapper .map:not(.active){display: none;}
    .secConMap .wrapper .map:before { content: ''; padding-bottom: 120.275%; display: block; }
    .secConMap .wrapper .map iframe { 
      position: absolute; top: 0; left: 0; width: 100%; height: 100%;
      -webkit-filter: grayscale(100%); -moz-filter: grayscale(100%); -ms-filter: grayscale(100%); -o-filter: grayscale(100%); filter: grayscale(100%); }

.scMap-tab { border-top: 1px solid rgba(0, 0, 0, 0.10); }
.scMap-tab:last-child { border-bottom: 1px solid rgba(0,0,0,.1); }
  .scMap-tab-title { padding: 40px 30px 40px 0; margin:0; position: relative; cursor: pointer; }
    .scMap-tab-title .icon { position: absolute; right: 0; width: 16px; height: 16px; top: calc(50% - 8px); transform: rotate(-45deg);
      transition: transform 0.5s ease; }
      .scMap-tab-title.comeSoon { display: flex; justify-content: space-between; pointer-events: none; padding-right: 0; }
    .scMap-tab-title .titleComeSoon { display: inline-block; margin: 0; font-weight: 450; text-transform: uppercase; color: #C79D5B; float: right; flex: 0 0 auto; padding-left: 15px; }
    .active .scMap-tab-title .icon { transform: rotate(0); }
  .scMap-tab .tabWrapper { border-top: 1px solid rgba(0, 0, 0, 0.10); padding: 30px 0 40px; color: rgba(var(--color-foreground), 1); display: none; }
  .scMap-tab.active .tabWrapper { display: block; }


.contact-time { display: flex; justify-content: space-between; margin: 0 -20px; position: relative; }
  .contact-time:before { content: ''; top: 0; left: 46.26168%; background: #000; opacity: 0.10; position: absolute; width: 1px; height: 100%; }
  .contact-time .item { flex: 1 1 auto; padding: 0 20px; }
  .contact-time .tabContact { max-width: 230px; }
  .contact-time .tabTime { max-width: 272px; }

.contact-time .item .ct-title { color: #C79D5B; font-weight: 450; text-transform: uppercase; margin: 0 0 16px; }
  .contact-time .rte p { margin-bottom: 12px; margin-top: 12px; }
  .contact-time .rte p:last-child { margin-bottom: 0; }
    .contact-time .rte a { --color-link: currentColor; --alpha-link: 1; }

.tabTime table { box-shadow: none; }
.tabTime table tr+tr td { padding-top: 8px; }
  .tabTime table td { border-width: 0; padding: 0; }
  .tabTime table td.h6 { text-transform: uppercase; padding-right: 10px; }
  .tabTime table td:last-child { font-size: 14px; font-style: normal; font-weight: 400; line-height: 150%; text-align: right; }

  .tabt-links { display: flex; border-bottom: 1px solid rgba(0, 0, 0, 0.10); margin-bottom: 30px; }
    .tabt-links .tabt-link-title { margin: 0; padding-bottom: 11px; margin-right: 20px; border-bottom: 1px solid transparent; margin-bottom: -1px; cursor: pointer; transition: all 0.5s ease; }  
    .tabt-links .tabt-link-title:hover,
    .tabt-links .tabt-link-title.active { color: #C79D5B; border-bottom-color: currentColor; }

  .contact-time + .tabtab { padding-top: 30px; border-top: 1px solid rgba(0,0,0,.1); margin-top: 30px; }

  .tabtab .tabt-content:not(.active) { display: none; }
  
  .tabt-content .rte p { margin-bottom: 20px; margin-top: 8px; }
  .tabt-content .rte strong { font-weight: 500; }
  .tabt-content .rte p:first-child { margin-top: 0; }
  .tabt-content .rte p:last-child { margin-bottom: 0; }


@media screen and ( max-width: 1199px ){
  .sec-contact-map .main-page-title { margin-bottom: 60px; }
  
  .secConMap .wrapper .content { width: 55%; padding-right: 30px; }
  .secConMap .wrapper .map-wrapper { width: 45%; }
}
@media screen and ( max-width: 989px ){
  .secConMap .wrapper { flex-wrap: wrap; }
  .secConMap .wrapper .content, 
  .secConMap .wrapper .map-wrapper { width: 100%; max-width: 100%; padding-right: 0; }
  .secConMap .wrapper .map-wrapper { margin-top: 40px; }
  .secConMap .wrapper .map:before { padding-bottom: 65vw; }
  @media (min-width: 750px){
    .sec-contact-map .page-width { padding-left: 3.2rem; padding-right: 3.2rem; }
  }
  .contact-time .item.tabTime { max-width: 385px; }
}
@media screen and ( max-width: 749px ){
  .sec-contact-map .main-page-title { margin-bottom: 40px; }
  
  .scMap-tab-title { padding: 30px 0; }
  .contact-time { flex-wrap: wrap; margin: 0; }
    .tabTime table { width: 100%; }
      .tabTime table td.h6 { width: 45%; }
  
    .item.tabContact,
    .contact-time .tabTime { max-width: 100%; width: 100%; padding-left: 0; padding-right: 0; }
    .item.tabContact { padding-bottom: 30px; margin-bottom: 30px; border-bottom: 1px solid rgba(0,0,0,.1); }
    .contact-time:before { display: none; }  
  
  .secConMap .wrapper .map:before { padding-bottom: 100%; }
}


